//! C ABI bridge for the editor UI component.
//!
//! Note: This is a skeleton crate. The ABI surface will be implemented
//! incrementally (see `editor-ui-component.md`).

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert_eq!(2 + 2, 4);
    }
}

