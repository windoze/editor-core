use editor_core_diff_view::{DiffColumnView, DiffModel, DiffProjection, Row, SideDoc};

#[test]
fn placeholder_types_are_constructible() {
    let _side = SideDoc;
    let _model = DiffModel;
    let _projection = DiffProjection;
    let _row = Row;
    let _view = DiffColumnView;
}
